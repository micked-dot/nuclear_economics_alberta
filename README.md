# nuclear_economics_alberta

This repository contains a stochastic electricity/gas economics model.

Usage:
- Run `original.py` to execute a single scenario and view the standard plots.
- Run `avg_of_iterations` to execute many iterations and print average results:
  - `python avg_of_iterations --iterations 100`
  - `python avg_of_iterations --iterations 100 --seed 42`
